version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: dtc-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: dtc_management
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
